SALIDO V18.0

Major fix: the Natural View photograph now uses a real <img> element as the single source of truth. Admin Preview and the live website therefore read the exact same image path. No CSS variable or inline-style synchronization is required.

Deploy the CONTENTS of this folder to the repository root. After deployment, hard refresh /admin/ and the public page.
