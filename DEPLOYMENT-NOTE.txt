SALIDO V18.0.1

Fix: Natural View image selection is applied immediately to the Admin draft, Preview, and published website. Each replacement uses a unique filename to bypass immutable browser and Cloudflare caches.

Deploy the CONTENTS of this folder to the repository root. Hard-refresh /admin/ after deployment.
