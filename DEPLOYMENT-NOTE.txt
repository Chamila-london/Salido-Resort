SALIDO V17.1.3 — FACILITY IMAGE LIVE-SITE HOTFIX

Fixes the Natural View image appearing in Admin Preview but not on the published Cloudflare/Netlify website.

Cause: the image path was stored in an inline style/custom property. The production security policy can block inline styles.

Fix: the Admin now writes the Natural View image URL into the external css/style.css file, which is allowed by the site CSP. Publishing an image now includes index.html, css/style.css and the WebP image.

Deploy every file inside this folder to the repository root.
