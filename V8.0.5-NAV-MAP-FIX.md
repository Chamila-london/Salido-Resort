# Salido Resort V8.0.5

- Locked all desktop navigation labels to the same font size, weight, line-height, and icon size in normal, hover, active, and sticky states.
- Removed active-state scaling so changing sections cannot make labels appear larger.
- Changed the destination map marker, pulse, label accent, and map action button to luminous lime `#66FF00`.
- No page content, admin logic, booking logic, or layouts were changed.
